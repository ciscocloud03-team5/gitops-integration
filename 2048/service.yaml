apiVersion: v1
kind: Service
metadata:
  name: app2048-service
  namespace: app
spec:
  selector:
    app: app2048
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP

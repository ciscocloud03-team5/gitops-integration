apiVersion: v1
kind: Service
metadata:
  name: ocr-service
  namespace: dev
  labels:
    app: ocr 
spec:
  selector:
    app: ocr
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000

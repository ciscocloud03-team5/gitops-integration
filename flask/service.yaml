apiVersion: v1
kind: Service
metadata:
  name: flask-service
  namespace: dev
  labels:
    app: flask 
spec:
  selector:
    app: flask
  type: ClusterIP
  ports:
    - name: flask-5000
      port: 5000
      targetPort: 5000

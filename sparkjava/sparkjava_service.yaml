apiVersion: v1
kind: Service
metadata:
  name: sparkjava-service
  namespace: dev
spec:
  selector:
    app: sparkjava-dev
  ports:
    - protocol: TCP
      port: 6026
      targetPort: 6026
  # following deployment container port 
  # type: ClusterIP
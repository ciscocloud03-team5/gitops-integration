apiVersion: v1
kind: Service
metadata:
  name: pokerogue-service
  namespace: dev
spec:
  selector:
    app: pokerogue
  ports:
    - protocol: TCP
      port: 6026
      targetPort: 6026
  # following deployment container port 
  # type: ClusterIP
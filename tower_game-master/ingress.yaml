apiVersion: v1
kind: Service
metadata:
  name: towergame-service
  namespace: app
spec:
  selector:
    app: towergame
  ports:
    - protocol: TCP
      port: 6026
      targetPort: 6026
  # type: ClusterIP